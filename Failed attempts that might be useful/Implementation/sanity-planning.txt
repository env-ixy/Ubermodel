So to implement ubermodel on a small dataset at its bare minimum for now, i will take a population of 20 models with only two parametrs and g = x+y and objective (chi) is -((x-3)^2 + (y-2)^2) a higher value of which would indicate a better score.

1. So firstly I will generate 20 models as 20 arrays with 2 elements that are random numbers between 0 and 1. Done. I used random.random here but random.uniform(0, 1) would've worked the same. NOTE: check with this later to see if anything imporves
2. Next I think I should define a function that taeks in the model and evaluates the first and second param as x and y respectfully and return it as a score. Done. ALso tested it on models and the scores are pretty concentrated around 10-13 with occastional spikes of 9 and 14-16. A few runs demonstrate this.
3. Next I think i should handle grouping. Since my choice for g here is simple, I can assign the f to each model very simply by just iterating over each model and summing the elements. Done, took a while but done.
4. Now for this instance i will set k as 1.0 but maybe later i will use the actual formula for it. and now to group i will use |f_i - f_j| < k but for multiple at once. This was a bit tough and I needed to use a bit of help from Copilot here. I had to change K to 0.01 because I realisd too late that any number from random.random < 1.0. So I think I'll define m (group size) as 4 for now, and make groups dependant on them.
5. Next I choose the winner from each group, that best fulfills the objective, which is pretty simple that I just need to maximum score.